export const MISSION_CATALOG_VERSION = 'api-migration-v1'

// Sourced from src/missions/sheet.source.json (catalog content version + last
// edit date). These describe the catalog payload, not the DB-keyed
// catalog_version used to look up rows.
export const MISSION_CONTENT_VERSION = '1.3.0'
export const MISSION_CONTENT_UPDATED_AT = '2026-05-14'
